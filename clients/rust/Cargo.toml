[package]
name = "hydra-umc-sdk"
version = "0.1.0"
edition = "2024"
description = "Rust reference client for HYDRA-UMC-SDK: hand-written types plus real JSON Schema runtime validation for the v1 contracts."
license = "GPL-3.0-or-later"

[dependencies]
jsonschema = "0.50"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
